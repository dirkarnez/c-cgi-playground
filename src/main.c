#include <stdio.h>

int main(void) {
  printf("Content-Type: text/plain;charset=us-ascii\n\n");
  printf("Hello world\n\n");
  
  /*
  size_t size = sizeof(buffer);
  fwrite(buffer, 1, size, stdout);
  */


  return 0;
}
